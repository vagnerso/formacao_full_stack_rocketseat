// Estrutura de condição
// operador condicional ternário
let age = 39
console.log(
    age >= 18 
    ? "Você pode dirigir"
    : "você não pode dirigir."
)

console.log(
    age < 18
    ? "Você não pode dirigir"
    : "Você pode dirigir"
)